import { metadata as reservationMetadata } from './metadata'

export const metadata = reservationMetadata

export default function ReservationLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 