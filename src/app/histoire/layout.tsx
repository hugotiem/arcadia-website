import { metadata as histoireMetadata } from './metadata'

export const metadata = histoireMetadata

export default function HistoireLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 